name: Verify Components - lindistflow_federate

on:
  push:
    paths:
      - 'Components/lindistflow_federate/**'
      - '.github/workflows/verify-components-lindistflow_federate.yml'
      - '.github/workflows/reusable_verify_components.yml'

jobs:
  verify:
    uses: ./.github/workflows/reusable_verify_components.yml
    with:
      component-root: Components/lindistflow_federate
