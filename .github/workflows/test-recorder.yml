name: Test - Recorder

on:
  push:
    branches: [ main, al/restructed ]
    paths:
      - 'Components/recorder/**'
      - '.github/workflows/test-recorder.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'Components/recorder/**'
      - '.github/workflows/test-recorder.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e Components/recorder[dev]

    - name: Run tests with pytest
      run: |
        pytest Components/recorder/tests/ -v --cov=recorder --cov-report=term
      env:
        PYTEST_ADDOPTS: "--color=yes"

    - name: Run type checking with mypy
      run: |
        cd Components/recorder
        mypy src/recorder --ignore-missing-imports
      continue-on-error: true
