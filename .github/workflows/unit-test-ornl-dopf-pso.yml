name: Unit Test - ornl-dopf-pso

on:
  push:
    branches: [ main, al/restructed ]
    paths:
      - 'Components/ornl-dopf-pso/**'
      - '.github/workflows/unit-test-ornl-dopf-pso.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'Components/ornl-dopf-pso/**'
      - '.github/workflows/unit-test-ornl-dopf-pso.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11']

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e Components/ornl-dopf-pso[test]

    - name: Run tests with pytest
      run: |
        pytest Components/ornl-dopf-pso/tests/ -v --cov=dopf_federate --cov-report=term
      env:
        PYTEST_ADDOPTS: "--color=yes"

    - name: Run type checking with mypy
      run: |
        cd Components/ornl-dopf-pso
        mypy src/dopf_federate --ignore-missing-imports
      continue-on-error: true
