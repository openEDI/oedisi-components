name: Verify Components - ornl-ev-pso

on:
  push:
    paths:
      - 'Components/ornl-ev-pso/**'
      - '.github/workflows/verify-components-ornl-ev-pso.yml'
      - '.github/workflows/reusable_verify_components.yml'

jobs:
  verify:
    uses: ./.github/workflows/reusable_verify_components.yml
    with:
      component-root: Components/ornl-ev-pso
