name: Unit Test - measuring_federate

on:
  workflow_dispatch:
  push:
    branches: [ main, al/restructed ]
    paths:
      - 'Components/measuring_federate/**'
      - '.github/workflows/unit-test-measuring_federate.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'Components/measuring_federate/**'
      - '.github/workflows/unit-test-measuring_federate.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11', '3.13']


    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e Components/measuring_federate[dev]

    - name: Run tests with pytest
      run: |
        pytest Components/measuring_federate/tests/ -v --cov=measuring_federate --cov-report=term
      env:
        PYTEST_ADDOPTS: "--color=yes"

    - name: Run type checking with mypy
      run: |
        cd Components/measuring_federate
        mypy src/measuring_federate --ignore-missing-imports
      continue-on-error: true
