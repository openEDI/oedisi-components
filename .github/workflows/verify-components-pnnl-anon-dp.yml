name: Verify Components - pnnl-anon-dp

on:
  push:
    paths:
      - 'Components/pnnl-anon-dp/**'
      - '.github/workflows/verify-components-pnnl-anon-dp.yml'
      - '.github/workflows/reusable_verify_components.yml'

jobs:
  verify:
    uses: ./.github/workflows/reusable_verify_components.yml
    with:
      component-root: Components/pnnl-anon-dp
