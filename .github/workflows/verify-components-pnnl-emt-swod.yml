name: Verify Components - pnnl-emt-swod

on:
  push:
    paths:
      - 'Components/pnnl-emt-swod/**'
      - '.github/workflows/verify-components-pnnl-emt-swod.yml'
      - '.github/workflows/reusable_verify_components.yml'

jobs:
  verify:
    uses: ./.github/workflows/reusable_verify_components.yml
    with:
      component-root: Components/pnnl-emt-swod
